server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info

# 敏感配置使用占位符并在运行时注入（遵循仓库安全规范）
spring:
  datasource:
    url: ${DB_URL:jdbc:h2:mem:testdb}
    username: ${DB_USERNAME:sa}
    password: ${DB_PASSWORD:}


